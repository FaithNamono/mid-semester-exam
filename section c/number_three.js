// Checks if  numbers are the same and returns their product. And if different returns their sum.
function calculate(a, b) {
    return a === b ? a * b : a + b;
  }
  console.log(calculate(3, 3)); // answer is 9
  console.log(calculate(3, 5)); // answer  is 8